<script lang="ts">
	import { m } from '$lib/paraglide/messages';

	let {
		message,
		emoji,
	}: {
		message: string;
		emoji: string;
	} = $props();

	//TODO banner at the top of the page (above navbar) for issues and stuff
</script>

<div class="fixed w-full"></div>
