import { RInteractive } from "./interactive";
import { RElement } from "./element";

export class RRenderer {
	setSize() {

	}	

	render(rie: RInteractive) {
		for(let e of rie.elements) {
			
		}
	}
};