import { describe, it, expect } from 'vitest';

//TODO encryption system tests
//DEK for courses, textbooks
//Public/Private for users

describe('security - user keys', () => {
	it('adds 1 + 2 to equal 3', () => {
		expect(1 + 2).toBe(3);
	});
});
